# simple python function that calculates the bubble sort of a list of numbers

function bubbleSort(numbers, steps=None)
    if steps is None:
        steps = len(numbers)
    for i in range(steps):
        for j in range(len(numbers) - 1):
            if numbers[j] > numbers[j + 1]:
                numbers[j], numbers[j + 1] = numbers[j + 1], numbers[j]
    return numbers

